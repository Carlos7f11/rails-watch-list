<h1><%= @list.name %></h1>
<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li><%= bookmark.movie.title %> - <%= link_to "Delete", list_bookmark %></li>
  <% end %>
</ul>

<%= link_to "Create a bookmark", new_list_bookmark_path(list_id: @list) %>
